# TexiolDexk Mobile v2.1.4 release notes

Mobile now falls back automatically to the free Showcase Relay when its same-LAN probe and direct WebRTC data channels cannot connect. The transport is visible in the session header. Frame chunks use the existing DXF2 assembler and are capped below the signalling message limit.

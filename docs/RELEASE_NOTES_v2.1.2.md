# TexiolDexk Mobile v2.1.2

This release fixes the blank remote screen after an Internet P2P connection opens.

Android now reassembles chunked DXF2 JPEG frames, buffers an early first frame until the viewer is ready, reports decode/assembly failures to the protected monitor and shows rendered-frame FPS. The previous signalling and same-LAN fixes remain included.
